import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

**File: `src/App.jsx`**
Copy the entire React component code from the artifact (the complete KnowledgeApp component)

**File: `.gitignore`**
```
node_modules
dist
.DS_Store
